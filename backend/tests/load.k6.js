import http from 'k6/http';import{check}from'k6';export const options={stages:[{duration:'30s',target:20},{duration:'1m',target:100},{duration:'30s',target:0}],thresholds:{http_req_failed:['rate<0.01'],http_req_duration:['p(95)<500']}};export default function(){const r=http.get(`${__ENV.BASE_URL||'http://localhost:4000'}/api/v1/products`);check(r,{'status 200':x=>x.status===200});}
